version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: next-portfolio:latest
    container_name: next-portfolio-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
